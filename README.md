# whatsapp-bomber

Desktop WhatsApp blaster (Fonnte gateway) — built with Rust + Tauri 2 + SQLite.

The initial project scaffold (modular folder structure, SQLite schema migration, and main
Rust logic for the Fonnte HTTP requests with async-delay) is being added in PR #1.
